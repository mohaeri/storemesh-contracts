import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const spec=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');test('container priority and damage replacement are published',()=>{assert.match(spec,/containers\/\{containerId\}\/priority/);assert.match(spec,/CONTAINER_PRIORITY_SET/);assert.match(spec,/containers\/\{containerId\}\/transfer-damaged/);assert.match(spec,/DAMAGED/);assert.match(spec,/CONTAINER_DAMAGED/)});
