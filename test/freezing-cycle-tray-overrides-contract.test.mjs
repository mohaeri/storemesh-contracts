import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('freezing cycle manager tray overrides are published',async()=>{const yaml=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');for(const token of['/api/cycles/{cycleId}/trays/add','/api/cycles/{cycleId}/trays/remove','CYCLE_TRAY_ADDED','CYCLE_TRAY_REMOVED','final tray cannot be removed'])assert.ok(yaml.includes(token),`missing ${token}`)});
