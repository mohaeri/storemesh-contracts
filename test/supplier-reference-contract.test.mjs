import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('supplier reference documents optional contact region and settlement fields',async()=>{const yaml=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');assert.match(yaml,/SupplierReference:/);for(const name of['contactName','contactPhone','contactEmail','region','settlementReference'])assert.match(yaml,new RegExp(`${name}: \\{type: string\\}`))});
