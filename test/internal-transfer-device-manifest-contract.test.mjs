import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('device manifest fetch is authenticated and exposes expected package codes by shipment id',async()=>{const yaml=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8'),route=yaml.match(/^  \/api\/internal-shipments\/\{shipmentId\}:.*$/m)?.[0]??'';for(const value of['shipment ID','expected package','signed manifest','shipping:write'])assert.match(route,new RegExp(value))});
