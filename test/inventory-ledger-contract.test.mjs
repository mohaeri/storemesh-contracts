import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
test('append-only inventory ledger endpoint and row fields are published',async()=>{const spec=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');for(const token of['/api/inventory/{entityType}/{entityId}/ledger:','BATCH, PACKAGE, FRESH_NET_LOT, FRESH_SHIPPING_BOX, CONSUMABLE','delta reason movementType userId unit beforeQty afterQty occurredAt sessionId deviceId siteId and sequence','userId is resolved server-side'])assert.ok(spec.includes(token),token)});
