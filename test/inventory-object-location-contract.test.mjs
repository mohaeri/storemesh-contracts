import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
test('inventory object movement correction and emergency relocation contracts are published',async()=>{const yaml=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');for(const token of['/api/inventory-items/{itemType}/{itemId}/move:','/api/inventory-items/{itemType}/{itemId}/emergency-relocate:','CONTAINER, PACKAGE, FRESH_SHIPPING_BOX','WAREHOUSE_LOCATION_CHANGED','EMERGENCY_RELOCATION','LOCATION_CORRECTION','second-manager approval','override:approve'])assert.ok(yaml.includes(token),`missing ${token}`)});
