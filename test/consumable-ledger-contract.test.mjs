import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const spec=readFileSync(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');test('consumable ledgers and validation errors are published',()=>{for(const x of['/api/consumables/{consumableId}/receipts','/api/consumables/{consumableId}/transactions','CONSUMABLE_RECEIPT_DATE_INVALID'])assert.ok(spec.includes(x),x)});
