import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const spec=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');test('receiving publishes configured harvest selection and error',()=>{assert.match(spec,/harvestPeriodId/);assert.match(spec,/HARVEST_PERIOD_NOT_CONFIGURED/)});test('transfer manifest carries harvest provenance',()=>{assert.match(spec,/manifest/);assert.match(spec,/Harvest|harvest/)});
