import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
test('shipment replacement and manager-gated assigned-item damage are published',async()=>{const yaml=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');for(const token of['/api/shipments/{shipmentId}/items:','SHIPMENT_ITEM_ADDED','replacesItemId','sales-order quantity','shipment:override','DAMAGE requires override:approve whenever shipmentId is set','when shipmentId is set override:approve is required'])assert.ok(yaml.includes(token),`missing ${token}`)});
