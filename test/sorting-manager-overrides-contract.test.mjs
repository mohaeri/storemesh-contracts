import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';const spec=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');test('sorting manager override routes and audit types are published',()=>{for(const value of['override-grade','override-destination','/priority','BATCH_GRADE_OVERRIDDEN','BATCH_DESTINATION_OVERRIDDEN'])assert.match(spec,new RegExp(value))});
