import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('fresh export manager override contracts publish priority split merge and audit semantics',async()=>{const yaml=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');for(const token of['/api/shipments/{shipmentId}/priority','/api/shipments/{shipmentId}/merge','/api/fresh-shipping-boxes/{boxId}/split','SHIPMENT_PRIORITY_SET','SHIPMENT_MERGED','FRESH_SHIPPING_BOX_SPLIT'])assert.ok(yaml.includes(token),`missing ${token}`)});
