import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('Finished Goods and carton manager overrides are published',async()=>{const yaml=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8');for(const token of['warehouseLocation','FINISHED_GOODS_TRANSFERRED','CARTON_REOPENED','REOPEN','override:approve','/api/packages/{packageId}/scans/remove','/api/packages/{packageId}/finished-goods'])assert.ok(yaml.includes(token),`missing ${token}`)});
