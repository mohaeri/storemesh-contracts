import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('shipment cancellation and duplicate errors are documented',async()=>{const yaml=await readFile(new URL('../openapi/storemesh.yaml',import.meta.url),'utf8'),create=yaml.slice(yaml.indexOf('  /api/shipments:'),yaml.indexOf('  /api/shipments/{shipmentId}/scans:'));assert.match(yaml,/SHIPMENT_CANCEL_REASON_REQUIRED/);assert.match(create,/SHIPMENT_CARTON_DUPLICATE/);assert.match(create,/SHIPMENT_BOX_DUPLICATE/);assert.doesNotMatch(yaml,/SHIPMENT_DUPLICATE_ITEM/);assert.match(yaml,/PICKING or READY cancellation additionally requires override approval/)});
